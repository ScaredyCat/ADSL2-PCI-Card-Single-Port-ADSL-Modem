
This is the base directory used for installation.
Some administrators would rather install softwares that are added to
an already existing system into /usr/local, since /usr is sometimes
mounted read-only.
Answering /usr is a safe bet anyway if you are not sure.

