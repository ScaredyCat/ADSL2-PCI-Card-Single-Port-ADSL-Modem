ifeq (sock.o skbuff.o iovec.o datagram.o scm.o sysctl_net_core.o dev.o ethtool.o dev_mcast.o dst.o neighbour.o rtnetlink.o utils.o netfilter.o wireless.o,$(strip $(subst $(comma),:,$(LDFLAGS) $(EXTRA_LDFLAGS) $(obj-y))))
FILES_FLAGS_UP_TO_DATE += core.o
endif
